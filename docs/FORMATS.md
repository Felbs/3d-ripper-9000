# Cracked formats - the running ledger

Every GameCube file format gcrip reads, grouped by the studio / engine that produced it.
"Rips" means textured models (and rigs / clips where noted) come out as glTF; "container"
means the archive is opened so the members reach the plugins and the fallback scanner;
"textures" means only the pixel data is understood. Each row names the module that
implements it and the note under [`docs/formats/`](formats/) that documents the byte layout
(the notes are the reverse-engineering record, written as the formats were cracked).

Chart of how these plug into the pipeline: [PIPELINE.md](PIPELINE.md) section 7.


What is **not** cracked yet, with what is blocking each one, is tracked in [OPEN.md](OPEN.md).

> **Writing a container plugin:** `is_container(name, head)` is offered only the first
> `gcrip.classify.SNIFF_BYTES` (64) bytes of a file, at both manifest and rip time.  Detect on
> the magic and validate the directory in `expand`, which gets the whole file - a check that
> reads a field beyond byte 64 silently never fires - see
> [formats/gcrip-plugin-sniff-limit.md](formats/gcrip-plugin-sniff-limit.md).
> A container also needs a no-op `detect`/`extract` pair: `all_plugins()` only registers a
> module that has both, so `is_container`/`expand` alone means the plugin is **never registered**
> and the archive is never expanded.  That is what kept every AFS disc at zero.


## Nintendo first party and second party

| Engine / format | Games | What we get | Modules | Notes |
| --- | --- | --- | --- | --- |
| J3D (BMD / BDL, BCK / BTK / BRK / ...) | Wind Waker, Mario Sunshine, Mario Kart DD, Pikmin 2, Luigi's Mansion props, Zelda TP, ... | rigs, skins, materials, clips, expressions | `gcrip/formats/j3d*.py`, `gcrip/export` | core pipeline, [PIPELINE.md](PIPELINE.md) |
| RARC / Yaz0 / Yay0 / TGC | most Nintendo discs | container + decompression | `gcrip/formats/rarc.py`, `yaz0.py`, `yay0.py`, `tgc.py` | |
| Retro Studios PAK / CMDL / MREA | Metroid Prime 1 & 2 | models, areas, textures | `gcrip/plugins/retro.py` | |
| HAL sysdolphin `.dat` (HSD) | Smash Melee, Kirby Air Ride, + Eighting's Naruto GNT | JOBJ rigs, meshes, textures, anims | `gcrip/plugins/hsd.py`, `gcrip/formats/hsd.py` | |
| Amusement Vision GMA / TPL / LZ | F-Zero GX, Super Monkey Ball 1-2 | models, textures | `gcrip/plugins/gma.py` | |
| Pikmin `.mod` | Pikmin 1 | rigged models | `gcrip/plugins/pikmin.py` | |
| Luigi's Mansion `.mdl` / `.bin` | Luigi's Mansion | characters, rooms | `gcrip/plugins/lm.py` | |
| Star Fox Adventures MODELS.bin / .tab | Star Fox Adventures | models with skeletons | `gcrip/plugins/sfa.py` | |
| Paper Mario TTYD | Paper Mario: TTYD | models | `gcrip/plugins/ttyd.py` | |
| Fire Emblem PoR pack / LZ10 | Fire Emblem: Path of Radiance | models | `gcrip/plugins/feporr.py` | |
| Wave Race offset bundles | Wave Race: Blue Storm | container | `gcrip/plugins/waverace.py` | |
| TPL / BTI textures, GX pixel formats | everywhere | textures | `gcrip/formats/gx_texture.py`, `tpl.py` | |

## Third-party engines (cracked 2026-08-27 .. 08-29)

| Studio / engine | Games in the library | What we get | Modules | Notes |
| --- | --- | --- | --- | --- |
| EA BIG / VIV / RefPack, Tiburon TERF, Black Box ZZDATA | ~70 EA discs | container, and `MMAP` textures.  **EA format 11 is `C8`** - GX leaves 11 undefined, but the pixels are exactly 8 bits each and the palette block declares 256 populated `RGB5A3` entries, which only indices need; it raised on 863 textures over four discs (847 on NASCAR Thunder 2003) before being mapped | `gcrip/plugins/ea.py`, `gcrip/formats/ea_terf.py` | [formats/ea-eagl-gamecube.md](formats/ea-eagl-gamecube.md) |
| EA Tiburon `TERF` + GCMP.LIB `LZH1` (`comp5`) + `TMdl` (`.ea3`) | Madden NFL 2002-08, NCAA Football 2003-05, NFL Street 1-2, NASCAR Thunder 2003 / 2005 (14 discs) | stadiums, props, sky domes, balls **and, since 2026-09-04, the players** (`PLADATA.DAT`: skinned lists open with indexed matrix loads and a matrix-index byte per vertex - Madden 06's 61 player models, 81,446 triangles) with normals, colours, UVs and the model's own named `MMAP` texture pack.  The codec was ported from Madden 06's DOL: MSB-first bitstream, 4-bit code lengths, deflate's tables, Adler-32 trailer | `gcrip/formats/ea_lzh1.py`, `ea_tmdl.py`, `gcrip/plugins/ea_tmdl.py` | [formats/ea-tiburon-comp5.md](formats/ea-tiburon-comp5.md), [formats/ea-tiburon-tmdl.md](formats/ea-tiburon-tmdl.md) |
| EA Redwood Shores `SHOC` `.hog` + `rcmp` (`Rdat`) + `OBG` terrain + `TXG` textures | Tiger Woods PGA Tour 2003, 2004 (x2), 2005 (x2), 06 | course terrain (400k+ triangles a hole) and named textures; the `rcmp` LZ was ported from the 2005 DOL | `gcrip/formats/ea_rcmp.py`, `shoc.py`, `ea_obg.py`, `ea_txg.py` | [formats/ea-shoc-hog.md](formats/ea-shoc-hog.md) |
| Hudson `hfs` + `PRS1` (LZSS) + RenderWare PI texture dictionaries | Frogger: Ancient Shadow, Frogger's Adventures: The Rescue | RenderWare clumps and worlds with paletted textures; the codec was read out of the DOL | `gcrip/formats/prs1.py`, `rw_pitxd.py`, `frogger_hfs.py`, `gcrip/plugins/frogger_hfs.py` | [formats/frogger-hfs.md](formats/frogger-hfs.md) |
| High Voltage `FSTA` `.jam` + `GGG` models + `AGM` material text + HVS `TPL` | The Grim Adventures of Billy & Mandy, Codename: Kids Next Door | levels, props, vehicles with normals, colours, UVs and bound textures; skinned characters in bone space | `gcrip/formats/hvs_ggg.py`, `hvs_agm.py`, `fsta.py`, `tpl_hvs.py`, `gcrip/plugins/hvs_ggg.py` | [formats/jam-fsta-hvs.md](formats/jam-fsta-hvs.md) |
| Blitz Games `.gcp` packs + actors + textures (every `_TBTexture` format per `bUploadTexture` in `bratz.elf`: RGBA8, RGB5A3, C8 / C4 over RGB565 or RGB5A3 palettes - format 17 was C8 + RGB565 -, CMPR, I4, RGB565, I8) | Bratz x2, Fairly OddParents x2, SpongeBob: Creature from the Krusty Krab, Pac-Man World 3, Zapper, Bad Boys, Cubix, Chicken Little, **Taz: Wanted (2002 layout, `taz_actor.py` - oracle-mapped: bbox/radius header, signature-anchored DLs, f32 normals)** | characters (with skeletons), props and lightmapped level meshes with CRC-bound textures; layouts from the DWARF in Bratz's ELF | `gcrip/formats/blitz_pack.py`, `blitz_actor.py`, `taz_actor.py`, `gcrip/plugins/blitz.py`, `blitz_actor.py`, `blitz_tbt.py` | [formats/blitz-gcp-gamecube.md](formats/blitz-gcp-gamecube.md) |
| Mass Media `BOLT` `.BLT` archives + "distill" models | Muppets Party Cruise, Shrek Super Party, Pac-Man Fever (Namco Museum 2002 reads but is 2D) | board tiles, props, HUD and character-shell meshes placed by their object matrices, with RGB565 vertex lighting and CMPR / C8 / C4 / RGBA8 textures bound through the archive's material list; codec and loaders read from Muppets' DWARF ELF and the Shrek / Pac-Man DOLs, three exporter generations (1.9.21, 1.9.18, 1.3.10) | `gcrip/formats/bolt.py`, `bolt_model.py`, `gcrip/plugins/bolt.py`, `bolt_model.py`, `bolt_mat.py` | [formats/bolt-mass-media.md](formats/bolt-mass-media.md) |
| Midway `SEC` `.ssf` archives + in-place RenderWare clumps | Mortal Kombat: Deception, Mortal Kombat: Deadly Alliance | arenas and fighters (56-joint skins) with every sampled texture bound; the directory was read from Deception's mapped ELF, and the RenderWare reader learned Midway's in-place geometry structs, `PAD32` filler, code-side texcoord fractions and Deadly Alliance's big-endian RW 3.2 payloads | `gcrip/formats/mk_ssf.py`, `gcrip/plugins/mk_ssf.py`, `rwstream.py`, `rwgc.py`, `gcrip/plugins/renderware.py` | [formats/mk-ssf-midway.md](formats/mk-ssf-midway.md) |
| Treyarch NGL `amalga_gc.pak` + `GCNM` mesh files + `GCNT` textures | Ultimate Spider-Man, Spider-Man 2 | the whole game out of one archive: city props and districts, `CITY_ARENA`, characters skinned on the CPU (`FastSkinS16` programs replayed for bones and weights, T-pose), levels; read from Ultimate Spider-Man's 26,808-symbol map | `gcrip/formats/ngl_gc.py`, `gcrip/plugins/treyarch_pak.py`, `ngl_mesh.py` | [formats/treyarch-ngl-gamecube.md](formats/treyarch-ngl-gamecube.md) |
| EA Black Box chunk streams, Underground (2003) generation | Need for Speed: Underground | cars and tracks: four-index corners (u8, u16 when VertexDescription is 1) over F32 positions, 20-byte texture stream entries with record-described JDLZ streams - read from `Speed.elf`'s DWARF and `eDataRender::Render`; Underground 2 / Most Wanted / Carbon unchanged | `gcrip/formats/ea_nfs.py` | [formats/ea-black-box-underground.md](formats/ea-black-box-underground.md) |
| Next Level Games GL `.glg` models + `.glt` `PTLG` bundles | Super Mario Strikers | characters, stadiums (323 placed models a dome), crowd and props; chunk units of packets over shared-index streams, read from `MarioSoccerR.elf` (DWARF 1 + map) | `gcrip/formats/nlg_gl.py`, `gcrip/plugins/nlg_glg.py` | [formats/nlg-strikers-gl.md](formats/nlg-strikers-gl.md) |
| EA Los Angeles 2002 `.msh` meshes + `.cpt` compartments | Medal of Honor: Frontline | props and level compartments with embedded `SHPG` materials, compartments binding through the level's art file; read from the shipped ELF's symbols (`CStaticMesh::Init`, `CCompartment::Init`) | `gcrip/formats/ea_la.py`, `gcrip/plugins/ea_la.py` | [formats/ea-la-frontline.md](formats/ea-la-frontline.md) |
| EA Los Angeles 2003-04 EAGL packets, `.msh` / `.cpt` wrappers + `.rtc` tails | Medal of Honor: Rising Sun, GoldenEye: Rogue Agent | EAGL objects with separately indexed streams (s16 / u16 positions, s8 or f32 normals, RGBA8, s16 uv) and unpointed display lists behind them; the wrapped ELFs get their symbol tables from the level's `symbols.rtc` / the compartment's `.rtc`; textures from `.gsh` / `.csf` / the `_Art.cpt` bundle | `gcrip/formats/eagl.py` (`_decode_packet_la`), `gcrip/formats/ea_la.py`, `gcrip/plugins/ea_la.py`, `gcrip/plugins/eagl.py` | [formats/ea-la-frontline.md](formats/ea-la-frontline.md) |
| EA Los Angeles `.dmf` cluster-skinned characters + `.skl` + `.tpk` | Medal of Honor: Frontline | bodies, heads, weapon arms in the rest pose over the level skeleton (`DMClusterSynthesizeMatrices`, `_matrix_rot_yzx`); textures from the `TPAC` packs | `gcrip/formats/ea_la.py` (`parse_dmf`, `parse_skl`, `tpk_shapes`), `gcrip/plugins/ea_la.py` | [formats/ea-la-frontline.md](formats/ea-la-frontline.md) |
| Edge of Reality `index.ind` + `.arc` - `MODL` / `TXFL` / `SHDR` members and the `Datasets` packs | The Sims 2, The Sims 2 Pets, The Sims, The Sims Bustin' Out, The Urbz, Shark Tale, Over the Hedge (7 discs) | characters and objects: strips over packed s16 arrays or GX display lists, CMPR / C4 / C8 / RGBA8 textures bound through shaders by name hash; every loader read by name from The Sims 2's mapped ELF | `gcrip/formats/edge_ind.py`, `edge_model.py`, `edge_dataset.py`, `gcrip/plugins/edge_arc.py`, `edge_dataset.py`, `edge_model.py`, `edge_tex.py` | [formats/edge-of-reality-arc.md](formats/edge-of-reality-arc.md) |
| EA Canada EAGL (`.ord` + `.orp` ELF) | FIFA 2002-2004, NBA Live 2003-04, NHL 2003-04, MVP, Def Jam, Fight Night, SSX | rigged, skinned models; textures from SHPG `.gsh` | `gcrip/plugins/eagl.py`, `gcrip/formats/eagl.py` | same note  The `.ord` tail is **`.orp` or `.orl`** - the same ELF remainder with or without a `u32` size prefix - and nine discs use `.orl`; reading only `.orp` raised on all 9,732 of their `.ord` |
| EA Sports EBO | NHL 2005/06, NBA Live 2005/06, FIFA 05, FIFA WC 2006, UEFA CL | models + rigs + textures (the sibling `.gsh` shapes: mixed-case `ShpG` tag and a one-image header variant; NBA Short3 open) | `gcrip/plugins/ebo.py`, `gcrip/formats/ebo.py` | same note |
| Capcom RE4 DAS / DRS / UDAS + BIN | Resident Evil 4 | rooms, characters, textures | `gcrip/plugins/re4.py` | |
| Ubisoft Jade `.bf` | BG&E, Prince of Persia x3 | levels, characters, textures | `gcrip/plugins/jade.py` | later Jade (King Kong) open |
| Neversoft PRE | Tony Hawk's Underground | container + models | `gcrip/plugins/neversoft.py` | |
| Point of View `PHM` models, earlier `s_model` versions (20-byte vertex: `s16` uv, position, normal/4096, then -1,-1) | Spawn: Armageddon, The Scorpion King | models with normals and uvs, materials named from the file's own inline texture names (`SPAWNTPAGE02`) - `SPAWN.PHM` gives 1,987 vertices and 3,488 triangles.  The index array is read as one strip, which scores better than any split; face-vs-vertex normal agreement tops out at 0.832 because the mesh is smooth-shaded, not because of seams | `gcrip/formats/phm.py`, `gcrip/plugins/phm.py` | [formats/spawn-toc-wad.md](formats/spawn-toc-wad.md) |
| Konami `KCEO ARCDT` `.arc` (16-byte magic, count, 0x800 alignment, `count * 36` directory, 36-byte entries of `name[24]`, sector and size) | Evolution Snowboarding | container (`gcrip/plugins/kceo_arc.py`) - 155 of 155 entries across four archives fit and are named; members are `BPXB` files | `gcrip/formats/kceo_arc.py` | [formats/cluster2-arc.md](formats/cluster2-arc.md) |
| Piglet `PIGGCN.pkd`: a chain of independent zlib streams, blocks padded to 16, assets spanning blocks | Piglet's BIG GAME | container (`gcrip/plugins/piglet_pkd.py`) - the chain covers all 232,370,273 bytes in 10,328 streams for 425 MB inflated, holding 936 CLUMP, 404 TEXDICTIONARY, 66 WORLD and 4,891 animations.  The existing RenderWare plugin claims all 1,001 geometry assets but returns a scene for **68 of them, 27,478 triangles** - the container is done, the reader is not | `gcrip/formats/piglet_pkd.py` | [formats/rws-is-audio.md](formats/rws-is-audio.md) |
| Toys for Bob `.gcn` resource archives (chunk chain: `0x071C` class census, `0x0716` named resource whose header length locates a RenderWare payload) | Madagascar | container (`gcrip/plugins/tfb_gcn.py`) - 46 of 49 `rwID_CLUMP` / `rwID_WORLD` / `rwID_TEXDICTIONARY` / `rwID_HANIMANIMATION` payloads, read by the existing RenderWare plugin: **114,936 triangles from `title.gcn` alone** on a disc that reported zero | `gcrip/formats/tfb_gcn.py` | [formats/rws-is-audio.md](formats/rws-is-audio.md) |
| Yuke's `.pac` / `.tex` packs | WWE Day of Reckoning 1-2, WWE WrestleMania XIX | container (`gcrip/plugins/yukes_pac.py`): flat LE name/type/size/offset table; the `.tex` packs (5,338 on DoR) hold plain TPL entries for the TPL plugin - 37 textures from one wrestler; the `ymg` YOBJ models are open | `gcrip/formats/yukes_pac.py` | OPEN.md (YOBJ row) |
| A2M `.ghr` levels (DTStreamFAT), HG objects / worlds (`DTBinaryPersistStream`), `.htd` dictionaries | Scooby-Doo! Unmasked, Scaler | characters with bones and skin weights, props, the level's static container and sky; textures CMPR / RGBA8 from the level's `.htd`; transcribed from the shipped `engine_ret.elf` symbol table (`Load3dObj`, `ImportSubSurfaces`, `ReadVertex`, `EFStatic3dObj::LoadFromStream` ...) - **60-80 scenes / 60-67k triangles a level, ~90% textured** on discs that reported 1.4k / 2.8k | `gcrip/formats/a2m_hg.py`, `gcrip/plugins/a2m_hg.py` | [formats/a2m-hg.md](formats/a2m-hg.md) |
| Point of View `PHM` models (`s_model` from the shipped DWARF: 44-byte f32 vertices, u32 index lists, 8-byte strip commands), `TIM` texture records and `TG_<phase>` layouts | Smashing Drive | every model textured by texture-def name (450 in `COMMON.WAD`); a phase assembles into one scene - the world-space buildings plus 700-1,900 placed props and traffic (63k-196k triangles) | `gcrip/formats/pov_model.py`, `gcrip/plugins/pov_model.py`, `gcrip/formats/pov_level.py`, `gcrip/plugins/pov_level.py` | [formats/pov-smashing-drive.md](formats/pov-smashing-drive.md) |
| Nintendo SDK character-pipeline `.gpl` geometry palettes (versions `0x005bbc61`, `0x00b749e0`) | Harvest Moon: A Wonderful Life / Another Wonderful Life, Zatch Bell! Mamodo Fury, Def Jam Vendetta, Doshin the Giant, Ultimate Muscle, Swingerz Golf, PoolEdge, Universal Studios Theme Parks Adventure | display objects: quantised GX arrays, vertex-descriptor states and GX lists, textures from the named `.tpl` (or the split ones beside a Harvest Moon character); a villager at 2.5k triangles, all textured | `gcrip/formats/nin_gpl.py`, `gcrip/plugins/nin_gpl.py` | [formats/nintendo-gpl.md](formats/nintendo-gpl.md) |
| A2M `.gcr` level archives (DTStreamFAT) + `TEXDIC_*.txd` image dictionaries | Scooby-Doo! Mystery Mayhem | container (`gcrip/plugins/a2m_gcr.py`): the RenderWare world and 100 clumps a level for the RenderWare plugin, which decodes the `(RW IMAGE chunk, name)` dictionaries beside the archive; **102 scenes / 77,715 triangles / 188 of 240 materials textured from one level** on a disc that reported 9.5k | `gcrip/formats/a2m_gcr.py` | [formats/a2m-gcr.md](formats/a2m-gcr.md) |
| Criterion RenderWare DFF / TXD / BSP, `.one`, HIP/HOP | Sonic Heroes, Shadow the Hedgehog, Heavy Iron titles, Bloody Roar, D.O.N | models, worlds, textures | `gcrip/plugins/renderware.py`, `gcrip/formats/one.py`, `rwstream.py`, `rwgc.py` | Shadow's `One Ver 0.60` in [formats/sonic-team-gamecube.md](formats/sonic-team-gamecube.md) |
| Radical Pure3D (`P3DZ` LZR, RCF archives) | Simpsons Hit & Run / Road Rage, Hulk x2, Crash Tag Team Racing, Dark Summit, Godzilla, Monsters Inc | meshes, skeletons, DDS/PNG textures (skin weights open) | `gcrip/plugins/p3d.py`, `rcf.py`, `gcrip/formats/p3d.py`, `lzr.py`, `rcf.py`, `dds.py` | [formats/radical-pure3d-gamecube.md](formats/radical-pure3d-gamecube.md) |
| Nintendo U8 archives (`.arc`: `55 AA 38 2D`, 12-byte nodes + string table) | Harvest Moon: A Wonderful Life / Another Wonderful Life, F-Zero GX (`vehicle_parts/parts_all.arc`), Swingerz Golf, Ultimate Muscle, One Piece: Treasure Battle - 12 discs, 1,414 archives | expanded as a container, members keep their in-archive paths (`gcrip/plugins/u8.py`, `gcrip/formats/u8.py`) | [formats/u8-and-loose-textures.md](formats/u8-and-loose-textures.md) |
| Loose RenderWare textures (`.dds` / `.tga` / `.tgx` named after the material) | MLB SlugFest 2003 / 2004, Outlaw Golf, RedCard 2003 | bound by `gcrip/plugins/renderware.py` through the new `gcrip/formats/tga.py` (SlugFest 0 -> 100% textured, Outlaw Golf 0 -> 92%) | [formats/u8-and-loose-textures.md](formats/u8-and-loose-textures.md) |
| Plain ZIP game data | Alien Hominid, Freedom Fighters, Hitman 2, NFL Blitz 2002 / 2003, Powerpuff Girls, Wallace & Gromit, X-Men Legends (8 discs) | expanded as a container (`gcrip/plugins/zip.py`); members are per-game formats (`.brec`, `.ovl`, `.igb`) | [formats/backlog-map.md](formats/backlog-map.md) |
| THQ `pack` archives | Avatar: The Last Airbender (9 archives, 700 MB), The Adventures of Jimmy Neutron (23, 1,013 MB) | expanded as a container (`gcrip/plugins/thq_pack.py`).  **Two versions share the magic**: version 1 has a 0x1c header and 16-byte entries, version 0 a 24-byte header and **12-byte** entries, told apart by the word at +4 and confirmed by `24 + count * 12` landing exactly on the name table.  **Avatar 9/9 archives, 245 members, 699 of 700 MB; Jimmy Neutron 23/23, 9,231 members, 1,003 of 1,013 MB** - including 2,007 `.rws` RenderWare streams, of which the sampled ones reach `renderware` and produce geometry | | [formats/backlog-map.md](formats/backlog-map.md) |
| Kashmir `.dat` scenes | City Racer, Taxi 3, Speed Challenge: Jacques Villeneuve's Racing Vision | models, placement, textures | `gcrip/formats/kashmir.py`, `gcrip/plugins/kashmir.py` | little-endian chunk stream (`kind, x, length`); f32 meshes with 13-byte triangles, nodes with euler placement, GameCube pictures behind a `RPMOC3S` header - also the discs' standalone `.tga`.  67k triangles / 138 textures on one City Racer track.  [formats/kashmir-dat.md](formats/kashmir-dat.md) |
| Neko NGDK `.GCN` (Cocoto x3, Charlie's Angels) | Cocoto Kart Racer, Funfair, Platform Jumper, Charlie's Angels | static worlds, textured | `gcrip/formats/neko_lz.py`, `neko_mwld.py`, `gcrip/plugins/neko_lz.py` | Okumura LZSS container; `MWLD` faces / s16 vertices / object vertex bases, `TIN.PC` index -> `GFX.PC` pictures (from the DOL loader) - a textured city block on Charlie's level 1.  `DYNO` props open - [formats/neko_lz.md](formats/neko_lz.md) |
| Midway Fang `.mst` | Freaky Flyers (2 discs) | container + textures; models via the GX scanner | `gcrip/formats/fang_mst.py`, `gcrip/plugins/fang_mst.py` | LZO1X members named by a 48-byte table; `.gtx` GX textures decode; `.gmo` / `.gmw` hold plain display lists - [formats/fang-mst.md](formats/fang-mst.md) |
| Acclaim `supertree0.tre` | Vexx, Turok: Evolution | container + textures | `gcrip/formats/acclaim_tre.py`, `gcrip/plugins/acclaim_tre.py` | hash-sorted 16-byte table over a 739-846 MB file; 32-byte-header CMPR / RGBA8 textures decode; models unlocated - [formats/acclaim-supertree.md](formats/acclaim-supertree.md) |
| Ubisoft `.fat` / `.000` + Sin Tzu `geoobj.bin` | Batman: Rise of Sin Tzu (models, textures), Batman: Vengeance (container) | models, textures | `gcrip/formats/ubi_fat.py`, `ubi_geoobj.py`, `gcrip/plugins/ubi_fat.py`, `ubi_geoobj.py` | LZO blocks with cross-block history; PC-layout (`x86`) strips with 36-byte vertices; linear DXT1 / RGBA8 / C4 pictures - [formats/ubi-fat-sintzu.md](formats/ubi-fat-sintzu.md) |
| EA `SHOC` `.scg` (STOC-indexed) | LotR: The Return of the King, The Third Age (2 discs) | terrain + textures | `gcrip/formats/shoc.py`, `ea_obg.py` (named elements), `ea_txg.py` (80-byte entries) | the Tiger Woods stream behind a zone index; 117k tris / 358 textures on one RotK level; `Cact` actors open - [formats/ea-shoc-hog.md](formats/ea-shoc-hog.md) |
| Neversoft PRE v2 (THPS3 / THPS4) | Tony Hawk's Pro Skater 3, 4 | textures | `gcrip/formats/neversoft.py` | 12-byte entries, little-endian on THPS3; `GCTX` pictures; scenes open |
| `res` resource files (tagged sections, end-of-file directory) | Digimon Rumble Arena 2, Lemony Snicket, Samurai Jack: The Shadow of Aku (3,804 files) | split into labelled sections by `gcrip/plugins/res.py`.  **`rdms` meshes decode** (`gcrip/formats/res_rdms.py`: a GX display list of five-`u16` corners, and **array offsets that are relative to their own header word** - the thing that made every earlier reading come out one element short) - **93,000 meshes and 7.4 million triangles across the three discs**, 96 / 93 / 96% of sections, 98% with uvs, **and since 2026-09-04 textured**: the word at rdms +4 is a self-relative link to the mesh's `gshd` shader, whose +0x5c links the `surf` it samples (73 of 73 on Samurai Jack's level files).  **Characters are `bmsh` sections, read since 2026-09-04** (`gcrip/formats/res_bmsh.py`: the bind-pose vertex buffer in model space, a `0x99` strip of four-identical-`u16` corners, blended slots copied from their per-bone runs, a shader a batch - the Scotsman in a textured T-pose, Digimon and Lemony Snicket characters at 99-100 % normal agreement).  **`surf` textures decode** (`gcrip/formats/res_surf.py`: GX C4/C8 with an `RGB565` palette, mip levels at +11, tile-padded chain) - 532 of 553 sections on Samurai Jack.  **`indx` is a name directory** (`res.index_entries`) - name and section offsets both **self-relative to their own field** - so sections carry their real asset paths (`menus/train_game/bobblehead_texture.tif`), 7 of 7 entries resolving to a section whose tag matches.  **`node` sections reference their meshes** (`res.node_links`, a 52-byte record with the same self-relative offset) - all 7 `rdms` on Lemony Snicket referenced exactly once by 3 nodes, so meshes are grouped and named by the object they belong to; the transform itself is still not read | [formats/res-rdms-meshes.md](formats/res-rdms-meshes.md) |
| Darkened Skye `PAK` archives | Darkened Skye (16 archives) | expanded as a container (`gcrip/plugins/skye_pak.py`): 44-byte records, and the entry table is located by requiring the members to tile.  **All 16 parse to 255 `.SKX` members**, and those now decode too | | [formats/darkened-skye.md](formats/darkened-skye.md) |
| `GCT` textures (`0xDEAD` header, GX mip chain) | Darkened Skye (4,340 files) | textures - **all 4,340 decode and the size matches exactly on every one**; CMPR 3,814, RGB5A3 440, I8 69, IA8 16, RGBA8 1 | `gcrip/plugins/gct.py`, `gcrip/formats/gct.py` | [formats/darkened-skye.md](formats/darkened-skye.md) |
| Darkened Skye `SKX` models | Darkened Skye (255 files) | meshes - an eight-word directory a mesh, **variable-length skinning records** (`u32 influences`, then `s16` position, `u16` joint and `f32` weight each) and 16-byte triangles carrying **two index triples into different arrays** (vertices and uvs).  Positions are `s16` over the directory's radius.  **255 of 255 parse - 532 meshes, 135,749 triangles**, none degenerate; the walk over the skinning records lands exactly on the triangle offset on every one | `gcrip/plugins/skx.py`, `gcrip/formats/skx.py` | [formats/darkened-skye.md](formats/darkened-skye.md) |
| Visual Concepts `DAT` archives | NBA 2K2, NBA 2K3, NFL 2K3, NCAA College Basketball 2K3, NCAA College Football 2K3 (5 discs, 5.5 GB) | expanded as a container (`gcrip/plugins/vc_dat.py`): each disc is **nine files**, one of them a 0.8-1.3 GB `game.dat` holding the whole game.  The entry table starts at **+36** (the word at +32 is the count), and offsets are relative to the 32-aligned end of a **name list** that follows the table - so members come out named.  **All five tile exactly: 9,380 members.**  The `.IFF` codec fell too (2026-09-04, `vc_pack` below); the `.BIN` / `.DAT` / `.CDF` members need no codec | | [formats/visual-concepts-dat.md](formats/visual-concepts-dat.md) |
| Free Radical `gct` textures | TimeSplitters 2, TimeSplitters: Future Perfect, Second Sight | textures - a 32-byte big-endian header whose **doubled width and height** are the check and whose **mip count is the high half** of the word at +16 (read as one `u32` a three-level texture reports format 196,613).  Free Radical's own format numbering, several codes to a format - 2/3/4/10/13 are `CMPR`, 5/7 `I8`, 6/8 `RGB5A3`, 0 `RGBA8`, settled by the **mip-chain ratio** (a full chain is four thirds of the top level) and confirmed by eye.  **6,409 of 6,465 sampled textures decode - 100 / 98 / 99% by disc** | `gcrip/plugins/frd_gct.py`, `gcrip/formats/frd_gct.py` | [formats/free-radical-pck.md](formats/free-radical-pck.md) |
| Free Radical `gcr` models - four shapes of one idea: a texture slot table, node records with batch tables, position / uv / normal / colour arrays and per-batch GX strip display lists of index vertices.  TS2 characters (0xa0 records before a trailer at the end), TS2 levels (the same records, found by the +0x9c back-pointer, 6-byte vertex-lit vertices), Future Perfect / Second Sight (0xc0 records, `s16 / 1024` positions and uvs, normals from a 4096-entry palette in the DOL, embedded or hash-named gct), and the array-block characters (f32 on TS2, s16 on FP / SS; strips re-wound per triangle) | TimeSplitters 2, TimeSplitters: Future Perfect, Second Sight | ships (`plugins/frd_gcr.py`; the TS2 and FP DOLs' draw routines read): **TS2 - all 314 of `chr.pak` (343,841 triangles), level 11 (51 sectors, 10,461), a story pak 46,437**; **FP - three prop paks 29 models / 33,250 triangles, no file declined**; **Second Sight - two cutscene paks 19 models / 20,712 triangles, 91 textures** | [formats/free-radical-pck.md](formats/free-radical-pck.md) |
| 007: Agent Under Fire `maps/*.ngc` - a 32-byte-aligned chunk stream (reversed 8-byte tags, BOA byte-LZ), the `bspfile` chunk a Quake III lump set whose `ngcsurfs` lump holds 14-byte `s16` vertices behind a 3x4 world matrix, `u8` strip indices and 28-byte surfaces; shaders and textures by `u32` id from the `shaders` / `restxtrs` chunks | 007: Agent Under Fire (38 maps, 273 MB) | ships (`plugins/auf_ngc.py`, read from `Bond.elf`'s symtab): **dm1 10,223 / alp1_1 17,412 / carrier1 26,346 world triangles, textured, plus the `restable` models - dm1's 124 `.gcm` (heads, weapons, props), 25,827 triangles**; patches and lightmap images are open | [formats/auf-ngc.md](formats/auf-ngc.md) |
| Free Radical `P4CK` / `P5CK` / `P8CK` archives | TimeSplitters 2, TimeSplitters: Future Perfect, Second Sight (637 files, 2.7 GB) | expanded as a container (`gcrip/plugins/frd_pak.py`): **two header meanings and three entry shapes, none of them declared** - the reader picks by arithmetic (the three words sum to the file length, or the table is the last `count * 12` bytes) and by requiring the members to tile.  The fourth word is the **stored** length and the third the unpacked one; where they differ the member is gzip and its real name is in the gzip header.  **149 sampled archives all parse - 24,539 members**, `gct` textures, `gcr` geometry, `war` animation | | [formats/free-radical-pck.md](formats/free-radical-pck.md) |
| `PNG` images | anywhere a container yields one - Aquaman, TONKA Rescue Patrol, FutureTactics | textures - gcrip has listed `PNG` among the magics it recognises since early on but had **no decoder**, so a PNG handed out by a container fell through to the fallback | `gcrip/plugins/png.py`, `gcrip/formats/png.py` | [formats/hff-carving.md](formats/hff-carving.md) |
| `TOC` `.wad` archives | Spawn: Armageddon (201 archives, 435 MB), The Scorpion King (200, 312 MB) | expanded as a container (`gcrip/plugins/toc_wad.py`): sixteen zero bytes, `TOC` at +16, then `count * 32` bytes of `name[16] | type[4] | offset | size`.  The Scorpion King uses **the same records with no table**, each behind a 28-byte wrapper with the next at `offset + 28 + size`.  **All 401 archives parse - 20,917 members** (Spawn 12,034, Scorpion King 8,883) - and the dead-ends list had these down as audio: 12,018 are textures against 286 sound | | [formats/spawn-toc-wad.md](formats/spawn-toc-wad.md) |
| `TIM` textures | Spawn: Armageddon (5,919), The Scorpion King (6,099) | textures - the name is Sony's but the format word is a **real GX code**, and `pixel bytes == encoded_size(format, w, h)` is the check rather than the picture looking right.  A palette-indexed texture keeps its palette in the member's tail.  **All 12,018 decode** - 11,832 `CMPR`, 185 `C8`, 1 `RGBA8` | `gcrip/plugins/toc_tim.py`, `gcrip/formats/toc_tim.py` | [formats/spawn-toc-wad.md](formats/spawn-toc-wad.md) |
| FutureTactics `files.pak` | FutureTactics: The Uprising (one 143 MB archive) | expanded as a container (`gcrip/plugins/ft_pak.py`).  **No magic** - recognised by its own arithmetic inside the 64 bytes `is_container` gets: the first entry's offset must equal `4 + count * 56`.  **Bit 31 of the size is a flag**, and reading it as a size puts 3,055 of 5,458 members two gigabytes past the end of the file.  **5,451 members, 150.6 of 150.9 MB**.  The 2,403 unflagged members are stored raw and **decode through the real chain** - 12 of 12 sampled `.DDS`, `.BMP` and `.PNG` each came back as textures; the 3,055 flagged ones are compressed with a private codec and come out packed, including every `.DFF` | | [formats/futuretactics-pak.md](formats/futuretactics-pak.md) |
| `HFF` data files | Aquaman: Battle for Atlantis, Casper, TONKA Rescue Patrol (3 discs, 594 MB) | expanded as a container (`gcrip/plugins/hff.py`).  **There is no directory**, so members are carved: `PNG` between its signature and `IEND`, and - since `casperGCN.elf`'s symbol table turned out to be RenderWare 3.0's (`readGeometryNative`, `WorldBuildMeshAtomicSector`) - **RenderWare streams walked header to header** (texture dictionaries, clumps, worlds, bare rasters with the 0x0800FFFF / 0x0C02FFFF stamps).  4,697 PNG textures from Aquaman and TONKA; Casper's first 16 MB give a 13,650-triangle world with 78 textures bound and 40 clumps, TONKA's middle 12 MB 104 clumps, Aquaman's head 34,212 triangles - all through the RenderWare plugin as it stands | | [formats/hff-carving.md](formats/hff-carving.md) |
| Windows `BMP` | 12 discs that produced nothing else (Aggressive Inline, Cel Damage, Dave Mirra Freestyle BMX 2, WRECKLESS, Crash Nitro Kart, X-Men, BMX XXX, Namco Museum, All-Star Baseball 2002, the Fairly OddParents, Taz, Zapper) | textures - 4 / 8 bit palette and 24 / 32 bit direct, rows bottom-up unless the height is negative.  **Anything over 4,096 on a side is refused**: two of these discs pad themselves out with 8500x8500 and 7960x8000 bitmaps, 216 MB and 191 MB apiece, that are not art | `gcrip/plugins/bmp.py`, `gcrip/formats/bmp.py` | |
| Acclaim `TBLOCKTEX` / `ASB_TEXTURE` | All-Star Baseball 2002, 2003, 2004 (1,570 `.tex`, 611 MB) | textures - no GX format is named anywhere, and none is needed: the **palette size and the bits per pixel determine it** (`C8`, `C4`, `CMPR`, `RGBA8`) and `encoded_size` then has to reproduce the stored byte count.  The palette follows the pixels, `TBLOCKTEX` palettes are `RGB5A3` and `ASB_TEXTURE` palettes `RGB565`.  **42,403 images decoded from three discs that produced nothing** | `gcrip/plugins/asb_tex.py`, `gcrip/formats/asb_tex.py` | [formats/acclaim-asb-textures.md](formats/acclaim-asb-textures.md) |
| `JAM2` archives | Charlie and the Chocolate Factory (38 `.jam`, 245 MB) | expanded as a container (`gcrip/plugins/jam2.py`): two string tables, then records of `u16 name | u16 extension | u32 offset` starting **four bytes after the extension table**, and each member's size written **twice at the member**.  **37 of 38 parse to 24,897 members covering 243 of 245 MB** - including **2,768 `TPL` textures** on a disc that yielded none, plus 1,097 `GMS` / 1,115 `GKA` / 1,204 `GGG`, the still-open High Voltage formats | | [formats/charlie-jam2.md](formats/charlie-jam2.md) |
| High Voltage `AGG` meshes (plain text) | Hunter: The Reckoning (2,114 inside the `LJAM` archives) | meshes - a brace grammar whose `VertexFormat` **states** the row width (`Pos3D`, `Normal`, `BlendWeight n`, `DiffuseColor`, `TxtCoord n`), `Index16Bit` naming a type and `Index[n]` counting **indices not faces**, and a `MeshComponent` slicing vertices and indices in different units.  **1,984 of 2,114 parse - 7,760 parts, 781,658 triangles**, 0.20% degenerate; the rest are collision maps with no index array | `gcrip/plugins/agg.py`, `gcrip/formats/agg.py` | [formats/high-voltage-ljam.md](formats/high-voltage-ljam.md) |
| High Voltage `LJAM` archives | Hunter: The Reckoning (35 `.jam`, 304 MB) | expanded as a container (`gcrip/plugins/ljam.py`): a depth-first tree whose every node is **its files and then its subdirectories**, two counted tables back to back.  **All 35 parse and the members cover 304 of 304 MB** - 22,662 of them, including **3,865 `TPL` textures** against the 566 the disc used to yield, plus 233 `TGA`.  Everything else is plain ASCII (`AGG` meshes, shaders, animation) | | [formats/high-voltage-ljam.md](formats/high-voltage-ljam.md) |
| Konami `KCEO ARCDT` archives | Evolution Snowboarding (29 `.arc`) | expanded as a container (`gcrip/plugins/kceo.py`): 36-byte records with a name, a 0x800-sector position and a size, and the members tile exactly.  **All 29 expand to 2,323 members** - `CUBE` `.SUR` (1,250) and `BPXB` `.BPX` (940), whose own formats are open | | [formats/konami-kceo-arc.md](formats/konami-kceo-arc.md) |
| Nintendo `TPL` textures, loose or embedded | everywhere; Mega Man X: Command Mission wraps one in a 32-byte header in each of 1,467 `.arc` | textures - gcrip has read TPL for years but only from inside game-specific plugins, so a loose or wrapped one was claimed by nothing.  `tpl.parse(data, base)` resolves every internal offset against the base | `gcrip/plugins/tpl.py`, `gcrip/formats/tpl.py`, `tpl_hvs.py` | [formats/capcom-arc-tpl.md](formats/capcom-arc-tpl.md) |
| `MDGC0200` blocks | Superman: Shadow of Apokolips (255 `.dgc`) | models: block type `0x1007`, a 64-byte header giving the vertex count and the offsets of a **GX display list**, the RGBA colours and the s8 normals; the list draws indexed triangle strips (position, colour and normal index per corner).  **947 meshes, 89,023 vertices, 130,937 triangles** | `gcrip/plugins/mdgc.py`, `gcrip/formats/mdgc.py` | [formats/superman-mdgc.md](formats/superman-mdgc.md) |
| `.jam` archives - three formats: `FSTA` (High Voltage, members open `HVSI`), `JAM2`, `LJAM` | Billy & Mandy + Kids Next Door (`FSTA`, 155 files), Charlie and the Chocolate Factory, Hunter: The Reckoning | `FSTA` expands (`gcrip/plugins/fsta.py`): members come out as `<name>.<ext>` - 477 from 20 archives on Billy & Mandy.  its `TPL` members are a High Voltage variant - extra `u32` at +8, image headers inline in the table at 0x2c stride - read by `gcrip/formats/tpl_hvs.py`, and loose stock TPLs anywhere are now claimed by `gcrip/plugins/tpl.py` | | [formats/jam-fsta-hvs.md](formats/jam-fsta-hvs.md) |
| `XMDL` / `NTGC` models (sections `MDEL`, `MATR`, `TXNM`, `GRPV`, `VRTX`, `INDX`; 32-byte big-endian f32 vertex) | Home Run King (`data.afs`: 69 members) | models - **6,273 models, 199,226 vertices, 155,356 triangles** | `gcrip/plugins/xmdl.py`, `gcrip/formats/xmdl.py` | [formats/afs-inner-formats.md](formats/afs-inner-formats.md) |
| Byte-swapped DDS texture packs (GX `CMPR` behind a big-endian DDS header, fourcc stored reversed as `1TXD`) | Home Run King (`data.afs`: 236 members, ~118 textures each) | textures | `gcrip/plugins/dds_pack.py`, `gcrip/formats/dds_pack.py` | [formats/afs-inner-formats.md](formats/afs-inner-formats.md) |
| Terminal Reality POD2 / POD3 archives | BloodRayne, Blowout, RoadKill (POD3), 4x4 Evo 2 (POD2) - 38 archives, 19,678 members | expanded as a container (`gcrip/plugins/pod.py`, `gcrip/formats/pod.py`); the `.bst` / `.bqs` models, `.tex` textures and `.pkg` packages inside are open | [formats/terminal-reality-pod.md](formats/terminal-reality-pod.md) |
| Terminal Reality `.TEX` textures | BloodRayne (3,685 / 3,785), Blowout (787 / 787) | textures: GX `CMPR` (code 11) and `C8` + 256-entry `RGB5A3` palette (code 19); the header is 24 bytes in version 2 and 28 in version 3 | `gcrip/plugins/tr_tex.py`, `gcrip/formats/tr_tex.py` | [formats/terminal-reality-pod.md](formats/terminal-reality-pod.md) |
| Terminal Reality `.PKG` packages | BloodRayne, Blowout, RoadKill | named chunks: `1tex` textures, `_smf` static meshes, `_dfm` skinned meshes, `_skl` skeletons, `smpl` audio (18/18 sampled packages walk clean to the `NoMo` terminator; 3,199 / 3,262 textures decode) | `gcrip/plugins/tr_pkg.py`, `gcrip/formats/tr_pkg.py` | [formats/terminal-reality-pod.md](formats/terminal-reality-pod.md) |
| Terminal Reality `.SMB` binary models - 4x4 Evo 2's `C3DModel` v1 (parts of a `CRenderPacket`: the 32-byte `SGCPacketHeader` with its fraction bits then a GX triangle list of 16-byte s16 vertices, or keyframed 32-byte f32 `SVertex` arrays) and RoadKill's `CModel` v6 (`Hunter.elf`: objects, collision meshes, materials, frames; every object header first and the payloads after, each a packet header + 13-byte vertices + a big-endian index list at the header's offset - the layout RoadKill's `.smf` share, which the `_smf` reader now walks instead of searching) | 4x4 Evo 2 (1,113 in `GCMODEL.POD/MODELS`), RoadKill (500 `.smb` + 734 `.smf`) | models: the Statue of Liberty 1,167 triangles; RoadKill's `CAR11` all 28 parts, 5,487 triangles (0 before); textures by the material's `.TIF` / `.RAW` name from the ART PODs (`.raw` is the `.TEX` layout, now claimed by `tr_tex` too) | `gcrip/formats/tr_smb.py`, `gcrip/formats/tr_cmodel.py`, `gcrip/plugins/tr_smb.py` | [formats/terminal-reality-pod.md](formats/terminal-reality-pod.md) |
| Terminal Reality `_smf` static meshes | Blowout (v7), BloodRayne (v4), RoadKill (v6) | models: GX display lists with INLINE vertices (quad opcode `0x84`); v7 is a 13-byte vertex (s16 pos * 2^-8, s8 normal, s16 uv), v4 a 16-byte one (s16 pos * 2^-15, Q1.14 s16 normal, u16 uv); degenerate triangles dropped and winding fixed from the stored normals | `gcrip/plugins/tr_smf.py`, `gcrip/formats/tr_smf.py` | [formats/terminal-reality-pod.md](formats/terminal-reality-pod.md) |  v6 is the indexed form of the same vertex (arrays + big-endian u16 indices) with a per-mesh power-of-two position scale taken from the stored bounding box.  Blowout's `_dfm` rigid-part characters remain open.
| Blitz `.gcp` packs | Pac-Man World 3, Bratz, Bad Boys, Chicken Little, ... (9) | archive directory cracked: 337 named per-level members (0x800-sector offsets + byte sizes), members split into stamped packages; object stream read by `gcrip/formats/blitz_obj.py` (tag 0x00 carries a byte rather than ending the stream - walks 99.7% of a pack); level packs are scene graphs, naming assets and navigation meshes, the 1,121 stampless `common_*` packs carry chained texture descriptors (`width | height | format | 0x101 | 0 | 0xff000000 | width*height` at 0x820); read by `gcrip/plugins/blitz_tex.py`: a chain of 160-byte descriptors from 0x820, each followed by its pixels - format 15 is GX `RGBA8`, 21 is GX `CMPR` (17 and 19 undecoded). Verified on Bratz: Rock Angelz and Fairly OddParents: Shadow Showdown, both previously at zero textures | `gcrip/plugins/blitz.py`, `gcrip/formats/blitz_gcp.py` | [formats/blitz-gcp-gamecube.md](formats/blitz-gcp-gamecube.md) |
| Krome RKV v1 + MDL2 `.gmd` + `.gtx` | Ty the Tasmanian Tiger | models, two-bone skins, textures | `gcrip/plugins/rkv.py`, `mdl2.py`, `gcrip/formats/rkv.py`, `mdl2.py` | [formats/krome-mdl2-gamecube.md](formats/krome-mdl2-gamecube.md) |
| Krome RKV2 + MDL3 / MDG3 + `.tex` | Ty 2, Ty 3, Spyro: A New Beginning, King Arthur | models, two-bone skins, textures | `gcrip/plugins/mdl3.py`, `gcrip/formats/mdl3.py` | same note |
| Krome RKV v1 + `GC01` `.mdl` / `.mdg` (the Merkury model before MDL3: `ModelTemplate` subobjects with 16-byte material records pointing at a span of the `.mdg`, which is GX display lists of 24-byte vertices - f32 position, s8 normal / 64, RGBA4 colour, s16 uv / 4096 - per `Model::ExploreBuildVertex` in `Jimmy.elf`) | Jimmy Neutron: Jet Fusion (1,317 pairs; the disc yielded 18 models) | models and levels: `room_6b_02` 68,616 triangles, props textured by the material's `.tex` | `gcrip/formats/krome_gc01.py`, `gcrip/plugins/krome_gc01.py` | [formats/krome-mdl2-gamecube.md](formats/krome-mdl2-gamecube.md) |
| Eighting FPK + PRS (GNTool variant) | Naruto CoN 1-2, GNT 3-4, Bloody Roar PF, Zatch Bell x2, Battle Stadium D.O.N | container -> HSD / RenderWare models | `gcrip/plugins/fpk.py`, `gcrip/formats/fpk.py` | [formats/eighting-fpk-gamecube.md](formats/eighting-fpk-gamecube.md) |
| Eurocom EngineX Filelist + GEOM `.edb` (v170-252) | Sphinx, Buffy, Spyro: A Hero's Tail, Robots, Batman Begins, Ice Age 2 | meshes, textures, assembled levels (map placements: 256 maps, 24,799 placements, 6.29M triangles, 5,988,747 triangles); rigs / map zones open | `gcrip/plugins/eurocom.py`, `gcrip/formats/eurocom.py` | [formats/eurocom-enginex-gamecube.md](formats/eurocom-enginex-gamecube.md) |
| Hudson `.bin` archives + HSF | Mario Party 4-7 | rigged, skinned models, textures | `gcrip/plugins/mpbin.py`, `hsf.py`, `gcrip/formats/mpbin.py`, `hsf.py` | [formats/hudson-hsf-gamecube.md](formats/hudson-hsf-gamecube.md) |
| Sega PRS, SA2B chunk models (big-endian Ninja), GVM / GVR | Sonic Adventure 2: Battle | rigged characters, textures (stages open) | `gcrip/plugins/segaprs.py`, `sa2b.py`, `gvm.py`, `gcrip/formats/prs.py`, `sa2b.py`, `gvr.py` | [formats/sonic-team-gamecube.md](formats/sonic-team-gamecube.md) |
| Nintendo REL modules + SA Tools split tables: SADX Basic models / land tables, SA2B stage land tables (Ginja) | Sonic Adventure DX, Sonic Adventure 2: Battle stages | characters rigged, stages textured (SADX stage textures open) | `gcrip/plugins/sadx.py`, `gcrip/formats/rel.py`, `satools.py`, `sadx.py`, `gcrip/data/satools/` | [formats/sonic-team-gamecube.md](formats/sonic-team-gamecube.md) |
| PSO BML, NJCM (GC order), GJCM "Ginja" | Phantasy Star Online Ep I & II (+ Plus) | rigged models, textures (`.rel` levels open) | `gcrip/plugins/bml.py`, `ninja_gc.py`, `gcrip/formats/bml.py`, `ginja.py` | same note |
| Traveller's Tales NU2 vertex stream (`03 01 00 01` blocks in NU20 `.gsc` / `.csc`) | LEGO Star Wars: The Video Game (chars + levels), Bionicle Heroes cutscene scenes | meshes with UVs, normals, colours (materials / textures open) | `gcrip/plugins/nu2.py`, `gcrip/formats/nu2.py` | [formats/tt-nu2-gamecube.md](formats/tt-nu2-gamecube.md) |

## Universal fallback

| Layer | What it does | Modules | Notes |
| --- | --- | --- | --- |
| Structure-based archives + zlib / LZ10 / LZ11 / LZSS | opens unknown tables and streams so real plugins see what is inside (never inside a real plugin's archive) | `gcrip/plugins/generic.py`, `gcrip/formats/generic.py` | [formats/library-fallbacks-2026-08-28.md](formats/library-fallbacks-2026-08-28.md) |
| GX display-list scanner | raw meshes from any file that stores GX display lists + vertex arrays | `gcrip/plugins/gx.py`, `gcrip/gxscan.py` | same note |

## Mapped, not decoded yet

| Studio / engine | Games | State | Notes |
| --- | --- | --- | --- |
| Ubisoft UE2 GameCube builds (`.umd` zlib archives of Unreal packages, `.lin`) | Splinter Cell x3 (5 dumps), Rainbow Six 3, Ghost Recon 2, XIII | Pandora Tomorrow rips (meshes, textures, assembled levels); the other titles' bundles are still being mapped | [formats/ubisoft-gamecube.md](formats/ubisoft-gamecube.md) |
| Ubisoft OpenSpace / CPA (`.lvl` + `.ptr`) | Rayman 3, Rayman Arena | reference loader exists (byvar/raymap) | same note |
| Traveller's Tales later / older variants: LSW2 & Narnia `.gcm`/`.cc2`, SMB Adventure `.chr`/`.chg`, LSW1 `.nus` scenes, `.ghg` | LEGO Star Wars II, Narnia, Crash WoC, Finding Nemo, SMB Adventure | containers seen, vertex encodings differ from the `.gsc` stream | [formats/tt-nu2-gamecube.md](formats/tt-nu2-gamecube.md) |
| Avalanche DBL / DBU / MDB: sub-database records (type byte 0x82 texture tables, 0x67 material lists, 0x2x meshes); a mesh = GX-native arrays (f32 positions, s8 normals, f32 uvs) + a list of raw GX FIFO display lists (index rows laid out by the stream's own VCD loads, per-DL material index + bone ids); textures CI4 / CI8 (RGB5A3 palette at the entry's palette offset), CMPR, RGBA8 | Tak and the Power of Juju, Tak 2, Tak: The Great Juju Challenge, Chicken Little, Dragon Ball Z: Sagas, Rugrats: Royal Ransom | ripped through `plugins/dbl.py` + `formats/dbl_mesh.py`; open: skeletons / skinning (only bone ids per DL) | [formats/avalanche-dbl-gamecube.md](formats/avalanche-dbl-gamecube.md) |
| Billy Hatcher `.prd` (PRS + `U:8-` archive), `.arc` (Ninja object trees with Ginja attaches, skin sets into the GX vertex cache, embedded / sibling GVM), `.lnd` (stage terrain: vertex pool + GX display lists) | Billy Hatcher and the Giant Egg | ripped - rigged characters, enemies, items, stage objects, terrain, textures (`plugins/billy.py`); the 79 `.lnd` terrain files were never detected until 2026-08-30 - `is_lnd` wanted 96 bytes and `detect` gets 64 ([formats/gcrip-plugin-sniff-limit.md](formats/gcrip-plugin-sniff-limit.md)); motions (`ge_player.arc`) open | [formats/sonic-team-gamecube.md](formats/sonic-team-gamecube.md) |
| Sega PSO `.rel` levels, Sonic Riders `80 00 00 01` containers | PSO, Sonic Riders | containers mapped | [formats/sonic-team-gamecube.md](formats/sonic-team-gamecube.md) |
| Traveller's Tales GameCube `DISP` display programs: `.csc` scenes / levels (big-endian NU2, reversed tags), `.chg` characters (skeleton wrapper), `.fpk` / `.cpk` packs | LEGO Star Wars II, The Chronicles of Narnia | ripped - textured meshes, vertex colours, level instancing, rigid-skinned LEGO characters (`plugins/ttdisp.py`); Narnia `.chg` skinning and `.hgo` (Crash WoC / Finding Nemo) open | [formats/tt-nu2-gamecube.md](formats/tt-nu2-gamecube.md) |
| Traveller's Tales reversed-tag NU2 (`FOGH` / `0CSG` trees, big-endian sizes): `.hgo` characters (HGO0: node matrices, meshes of `u32 blocks | u32 material | u32 count` + 28 / 36-byte f32 vertices `xyz | normal | RGBA | [uv]`, index groups prim 5 list / 6 strip, per-vertex skin `3 f32 weights + 4 bone ids`), `.nus` levels (GST0 meshes + INST 4x4 placements), TXM0 textures (0x80 CMPR, 0x81 RGB5A3), MS00 / MS03 materials | Crash Bandicoot: The Wrath of Cortex, Finding Nemo | ripped through `plugins/hgo.py` + `formats/hgo.py` (bind pose, flat joints; the Nemo levels use normal-less vertices and raw GX display lists; node hierarchy / bind matrices and unskinned part transforms open) | [formats/tt-nu2-gamecube.md](formats/tt-nu2-gamecube.md) |
| Unreal Engine 2 packages (Ubisoft GameCube builds): `.usx` static meshes, `.utx` textures, `.umd` / `.lin` chunked-zlib package archives | Splinter Cell: Pandora Tomorrow (ripping); Splinter Cell 1 / Chaos Theory / Double Agent, Rainbow Six 3, Ghost Recon 2, XIII (containers + LE tables) | Pandora Tomorrow static meshes, DXT1 / P8 textures and assembled levels rip (`plugins/unreal.py`: every actor with a `StaticMesh` property placed by Location / Rotation / DrawScale over the sibling `.usx` - 34 maps, 11,830 actors, 819k triangles, 0 missing meshes); the `.lin` / `.umd` bundles hold package headers with sequential name / import / export tables and the object data in separate blocks, so SC1 / Chaos Theory / Double Agent / XIII / R6-3 / GR2 still do not rip; BSP (`Model` / `Polys`) not exported | [formats/ubisoft-gamecube.md](formats/ubisoft-gamecube.md) |
| Ubisoft OpenSpace / CPA levels: `.lvl` + `.ptr` relocated memory images, super-object tree, GeometricObjects with GX strip mappings, Nintendo TPL textures | Rayman 3: Hoodlum Havoc, Rayman Arena | ripped - placed level geometry with textures (`plugins/openspace.py`); characters (Perso families / animations) open | [formats/ubisoft-gamecube.md](formats/ubisoft-gamecube.md) |
| Konami TMNT 1-3: `AFS` archives (`TMNT.DAT`, named members), TMNT 2 / 3's `LPAC` packs of named RenderWare streams, little-endian RenderWare 3.x `.dff` clumps / worlds / `.anm`, and Konami texture packs (chunk 0x23: TMNT 1 name table + rwID_IMAGE, TMNT 2 rwID_IMAGE + rwID_TEXTURE) | Teenage Mutant Ninja Turtles 1, 2: Battle Nexus, 3: Mutant Nightmare (5 discs), TMNT: Mutant Melee (`archive.dat` + its `archive.arc` directory) | ripped through `plugins/renderware.py` + `plugins/afs.py` + `plugins/lpac.py` + `formats/konami_pac.py` + `plugins/melee.py` (Mutant Melee: 2,331 rigged scenes / 615k triangles) | [formats/konami-gamecube.md](formats/konami-gamecube.md) |
| Konami others (Frogger, Yu-Gi-Oh FK, Disney Sports, ESPN, Evolution) | 12 | containers seen, one engine per title | [formats/konami-gamecube.md](formats/konami-gamecube.md) |
| Treyarch (Spider-Man 1-2, Ultimate Spider-Man, ...) | 7 | parked by decision | |
| Warthog `WART3.00` `.hog`: 24-byte header, 24-byte records at +24, two-section name table (directories then files); members carry a `u32` packed-length prefix, and a record with `packed == 0` is stored at its unpacked size | Animaniacs, Looney Tunes: Back in Action, Harry Potter and the Sorcerer's Stone | **container ships** (`plugins/wart_hog.py`) - **the member codec is solved**: four token forms with 10-, 14- and 16-bit windows.  98-99% of members decompress on all three discs (Animaniacs 4,582/4,663, Harry Potter 290/292, Looney Tunes 857/859).  **`.btga` textures ship too** (`plugins/wart_btga.py`, 96-byte header then a GX mip chain; code 0x01 CMPR, 0x81 IA4) - 26 of 26 on `frontend.hog`.  **`.bmsh` meshes ship** (`plugins/wart_bmsh.py`, section-table chain then GX display lists with a derived index stride) - **16 of 16** sampled members including the skinned characters, 6,672 triangles with positions and UVs, each confirmed against the header's own bounding volume.  Per-column index widths (`u16` position with `u8` texcoord in one vertex) and zero-length sections are both attested; what remains | [formats/warthog-hog.md](formats/warthog-hog.md) |
| Climax `.bad` + `.bah`: whole-game archive - the `.bah` (`Bark 1.06`) is a directory tree of names, offsets and sizes; every member is its own block (`u32 kind, u32 count`: raw, or ring-buffer LZSS to exactly `count` bytes - `cLZSS::Decompress` in the Hot Wheels DOL, which its `.map` names) | ATV: Quad Power Racing 2 (2,113 members), Hot Wheels World Race (6,796), The Italian Job (2,591) | every member named and inflated to its declared size (746 of 746 in ATV's first 6 MB): `.bog` textures, `.rom` models, `.row` worlds, `.kdt` collision, `.cuban` animations, text params | `gcrip/formats/climax_bad.py`, `gcrip/plugins/climax_bad.py` | [formats/climax-bad.md](formats/climax-bad.md) |
| Climax `.bog` textures (`BOG 1.01` 40-byte header, `BOG 1.02` 56-byte: Blimey format code, width, height, mips, image bytes; C8 / C4 carry an ARGB8888 palette before the GX tiles) | ATV (983), Hot Wheels (3,575), The Italian Job (1,499) | textures: car liveries, track sets | `gcrip/formats/climax_bog.py`, `gcrip/plugins/climax_bog.py` | [formats/climax-bad.md](formats/climax-bad.md) |
| Climax `.rom` models (`ROM 1.26` / `1.27`: `cModelFile` - 140-byte material records naming the `.bog`, attachment points, then meshes of u32 triangle triples over 56-byte f32 vertices, or 672-byte bicubic Bezier patches, tessellated 4x4) | ATV (380), Hot Wheels (1,282), The Italian Job (325) | models: ATV bodies 1,156-1,729 triangles with 21-34 patches, textured by material name | `gcrip/formats/climax_rom.py`, `gcrip/plugins/climax_rom.py` | [formats/climax-bad.md](formats/climax-bad.md) |
| Climax `.row` worlds (`ROW 2.25` / `2.26`: texture table naming the `.bog`s, seven side tables, then a depth-first node tree - 48-byte nodes, leaves holding 48-byte mesh headers over u32 triangles, 52-byte f32 vertices with three uv sets and RGBA, and 604-byte bicubic patches with corner uvs) | Hot Wheels (21 tracks), ATV (19), The Italian Job (7) | worlds: Hot Wheels `clng` 225 nodes, 765 meshes, 1,052 patches, 43,971 triangles at 0.99 normal agreement - track, terrain, refinery | `gcrip/formats/climax_row.py`, `gcrip/plugins/climax_row.py` | [formats/climax-bad.md](formats/climax-bad.md) |
| EA `SHOC` chunk archives (`.hog`): `CTRL` file header, `SHOC` wrappers pairing an `SHDR` with the data chunks after it.  **The inner tag is the storage mode** - `SDAT` stored, `Zdat` zlib, `Rdat` EA LZ behind a `u32` uncompressed size - each after a 44-byte chunk header | Tiger Woods PGA Tour 2003, 2004, 2005, 06 (872 archives) | container ships (`plugins/shoc.py`) - 120 of 120 sampled archives parse, 7,482 members, 99.998% of a 5.8 MB `hole.hog` covered.  On 06 the inner `OBG ` terrain and `TXG ` textures are ripped; on 2003/2004/2005 the archives hold **audio and camera config only** - their textures are the 41 headerless `.fxg` | [formats/ea-shoc-hog.md](formats/ea-shoc-hog.md) |
| Sony `TIM2` textures (8- and 4-bit indexed, CLUT entry width measured not assumed, CSM1 unswizzle, PS2-linear pixels) plus the offset table that holds them | Auto Modellista, Capcom vs SNK 2 EO (inside AFS members) | ripped - 25 of 28 pictures decode (`plugins/tim2.py`) | [formats/sony-tim2.md](formats/sony-tim2.md) |
| *(pipeline fix)* textures-only scenes - a Scene with images and no materials | 13 plugins: `asb_tex`, `blitz_tex`, `bmp`, `dds_pack`, `frd_gct`, `gct`, `gvm`, `png`, `res`, `tim2`, `toc_tim`, `tpl`, `tr_tex` | **their textures were decoded and never written** - the exporter only wrote textures a material named.  Fixed in `ripcore/gltf.py` | [formats/textures-only-scenes.md](formats/textures-only-scenes.md) |
| Terminal Reality `_dfm` header + part table (`u32 part count`, `u32 bone count`, the skeleton's file name at +24, then `char name[30]; u32 bone; f32 box[6]` at a 58-byte stride) | BloodRayne, Blowout, RoadKill | part table reads (`formats/tr_dfm.py`) - names 59/59 and 23/23, bone indices all inside the skeleton, **`min<=max` on every box**, and the parts name the bones they hang off (`binoculars2` -> `Bip01 L Hand`).  Geometry still open; the boxes are the oracle for it | [formats/terminal-reality-dfm.md](formats/terminal-reality-dfm.md) |
| Terminal Reality `.SKL` skeletons (`u32 version`, `u32 bone count`, then `char name[32]; s32 parent` at a 36-byte stride, padded with `BAADF00D` rather than zeroes) | BloodRayne, Blowout, RoadKill (inside `gcpkg.pod` -> `.PKG`) | hierarchy reads (`formats/tr_skl.py`) - 82 of 82 and 68 of 68 names decode, one root, **no parent points forward**, tree anatomically correct.  No bind transforms in the record, so the `_dfm` vertex layout is still open | [formats/terminal-reality-skl.md](formats/terminal-reality-skl.md) |
> **Identities are executable.**  The arithmetic each row quotes - "200 of 200", "12 of 12",
> "exact on 17 of 17" - is declared in code as `IDENTITIES` on the format module and checked by
> `gcrip/identities.py`.  A claim that stops holding fails the build instead of turning into a
> disc that quietly produces less.  See `tests/test_identities.py`.

| Frogger `hfs` archive - 67 directory blocks, 4,195 members; members carry their unpacked size at +4 | Frogger: Ancient Shadow, Frogger's Adventures: The Rescue (same archive, version byte 7) | archive reads (`formats/frogger_hfs.py`) - the table ends exactly where the data begins, the blocks chain by span 66 of 66, and the two account for all 197,959,680 bytes.  Members are `PRS1` and that codec is still unread | [formats/frogger-hfs.md](formats/frogger-hfs.md) |
| Call of Duty: Finest Hour `.rws` - RenderWare sections behind an 8-byte header | Call of Duty: Finest Hour (231 files, 582 MB) | ships (`formats/cod_rws.py`, `plugins/cod_rws.py`) - the walk accounts for the file to a padding word, and one 510 KB level gives **14,957 triangles** through `renderware.py` | [formats/cod-finest-hour-rws.md](formats/cod-finest-hour-rws.md) |
| EA's RenderWare stream container - `0x071C` type table, `0x0716` asset descriptors | Call of Duty: Finest Hour, Harry Potter: Goblet of Fire | reads (`formats/ea_rws.py`) - the walk covers the file exactly, 554 chunks on one and 6,411 on another, and 58 of 58 assets name an `rwID_` type.  No plugin yet: the assets it declares are splines, not meshes | [formats/ea-renderware-stream.md](formats/ea-renderware-stream.md) |
| `gxscan` salvage pass - re-walks the span a rejected chain claimed, ranked, gated on a first-pass hit | the universal fallback, every disc | ships (`gcrip/gxscan.py`) - benchmarked over 22 files: **5,738 -> 23,412 triangles**, empty files unchanged in time | [formats/gxscan-salvage.md](formats/gxscan-salvage.md) |
| RenderWare GameCube **native** geometry - GX display lists in a CLUMP's unchunked tail | Piglet's BIG GAME (936 clumps), and the other RenderWare GameCube discs to be checked | ships (`formats/rw_native.py`, `plugins/rw_native.py`) - **289,811 triangles from 146 sampled clumps** across the two readers, 0.2 s; normals unit to 8.48e-08 | [formats/renderware-native-gc.md](formats/renderware-native-gc.md) |
| HSD shared-subtree guard - a joint reachable by many paths is walked once | every disc the `hsd` plugin touches | fixed (`formats/hsd.py`) - One Piece's `l_result_back.dat` hung a library shard forever with no error; now 0.12 s and 6 scenes | [formats/hsd-shared-subtree.md](formats/hsd-shared-subtree.md) |
| Pokemon `FSYS` model members - a HAL sysdolphin `.dat` archive behind a 64- or 3,680-byte prefix | Pokemon Colosseum, Pokemon XD | ships (`formats/fsys.py` locates it, `plugins/hsd.py` reads it) - the member's first word is the archive's size and the HSD header has to reconcile with it; 813-1,356 triangles and up to 66 joints a Pokemon, over 1,130 `pkx` archives across the two discs | [formats/fsys.md](formats/fsys.md) |
| Acclaim `.GDF` meshes - GX display lists behind an attribute block located by the record counts | All-Star Baseball 2002, 2003, 2004 | ships (`formats/acclaim_gdf.py`, `plugins/acclaim_gdf.py`) - the declared bounding radius is the largest decoded `|v|` on 5 of 5 meshes, and the groups' declared triangle counts come out exactly (1,274 of 1,274).  `.SKN` is declined | [formats/acclaim-gdf.md](formats/acclaim-gdf.md) |
| Terminal Reality `_dfm` skinned meshes (versions 2 and 5): big-endian 1-4 bone records of 20/29/38/47 bytes inside a little-endian file - s16 positions per bone / 2^scale, s16 weights, s16 normal / 32768, u8 bone indices, u16 uv / 1024 - transcribed from `APIDLLpolyListGCBoneVertex` in the shipped `bloodrayne.elf` / `Blowout.elf` | BloodRayne (228, v2), Blowout (93, v5) | skinned characters in the home pose over the `.SKL` of the same package (soldier 3,914 triangles / 82 bones, Kane 2,957 / 29), weights and joints on every vertex, textured by the material's `.TIF` | `gcrip/formats/tr_dfm.py`, `gcrip/plugins/tr_dfm.py` | [formats/terminal-reality-dfm.md](formats/terminal-reality-dfm.md) |
| Visual Concepts `.IFF` codec inside `game.dat` - flag byte, then a 24-bit match word split 10:14 (length : distance+1) | NBA 2K2, NBA 2K3, NFL 2K3, NCAA College Basketball 2K3, NCAA College Football 2K3 | ships (`formats/vc_pack.py`) - **input-driven with a trimmed zero tail** (2026-09-04): the u32 at +21 is the first record's size, not the output length, members may hold several records, and the encoder trims trailing zero-producing ops (sometimes mid-word).  359 of 359 sampled members decode with zero errors, every decoded tag matches, 239 of 251 `RTXT` members tile into complete records exactly (the rest carry non-`RTXT` chunks after record one) | [formats/visual-concepts-dat.md](formats/visual-concepts-dat.md) |
| Kalisto `TotemTech` `.dgc` + `.ngc` pair - a plain-text hash index beside a byte-packed record chain | Spirits & Spells, Jimmy Neutron: Boy Genius, SpongeBob: Revenge of the Flying Dutchman | ships (`formats/totemtech.py`, `plugins/totemtech.py`) - 3,673 records walked on one level, **52 of 52 meshes, 21,142 triangles**; normals unit to 7.7e-07 and every strip index inside its vertex array | [formats/totemtech-dgc.md](formats/totemtech-dgc.md) |
| Cabela's `MULA` texture archives (`u32 count`, `{size, name offset}` pairs, a string table, then payloads tiling the block) holding `GCT ` images (`u16 w/h/format`, mip levels at +12, pixel bytes at +16, 32-byte header) | Cabela's Dangerous Hunts 2, Big Game Hunter 2005, Outdoor Adventures (in `Data/data.arc`) | ships (`plugins/cabelas_arc.py` + `plugins/mula.py`) - payload tiling exact on 2 of 2 whole blocks, `32 + palette + pixel bytes == size` on **200 of 200** textures, 282 decoded from the sample | [formats/cabelas-mula.md](formats/cabelas-mula.md) |
| EA `TXG` texture groups (`TXHE` 88-byte headers + `TXDA` pixels; size field excludes the header) | Tiger Woods PGA Tour 06 (the `txf` members of the `SHOC` archives) | ripped - 2,073 textures, 100% of those found (`plugins/ea_txg.py`) | [formats/ea-txg.md](formats/ea-txg.md) |
| Crystal Dynamics `bigfile.dat`: count, sorted hash table, 16-byte records with a `0xffffffff` sentinel, offsets in 2048-byte sectors, zlib or stored members | Tomb Raider: Legend (1.29 GB, the whole game in one file) | container ships (`plugins/cd_bigfile.py`) - 617 of 617 sampled members read exactly.  Payload formats (`00 00 00 0e`, `!WAR`) still open | [formats/cd-bigfile.md](formats/cd-bigfile.md) |
| *(audit)* plugins that claim a disc's files but produce nothing - the dead-plugin signature | 301 model-less discs checked | found `toc_wad`'s inline variant unclaimed (Scorpion King, 6,099 textures) and a third disc for it (Smashing Drive, 2,655) | [formats/dead-plugin-audit.md](formats/dead-plugin-audit.md) |
| ZIP archives whose central directory does not point at their data - walked by local header instead, CRC-checked | NFL Blitz 20-03, Blitz 20-02 | **16,090 members recovered, ~2,780 RenderWare `.dff`** (`formats/zip_local.py`); both discs produced nothing before | [formats/zip-local-walk.md](formats/zip-local-walk.md) |
| RenderWare files whose library id is a bare version (`0x0310`, `0x0304`) rather than a packed build stamp | NFL Blitz 20-03, Blitz 20-02, Burnout, Driven, Outlaw Golf, Redcard 20-03, MLB Slugfest 2003 / 20-04 | **were refused by the sniff and fell to the structure scanner**; 60 of 60 sampled parse perfectly.  2,783 `.dff` now detected on the two Blitz discs alone | [formats/renderware-plain-stamp.md](formats/renderware-plain-stamp.md) |
| EA `OBG` terrain: `ARRA` typed arrays (`(type<<24)|count`, `components<<18`) + `ELDA` u16 triangle strips with 0xffff restarts | Tiger Woods PGA Tour 06 (the `ter` members of the `SHOC` archives) | ripped - **4,814,090 triangles from 12 of 276 archives** (`plugins/ea_obg.py`) | [formats/ea-obg.md](formats/ea-obg.md) |
| Visual Concepts `RTXT` texture records: fixed records inside the uncompressed `.IFF` members of `game.dat`; name at +64, dimensions after it, **row-major** 8-bit indices at +176 and a 256-entry `RGB565` palette in the last 512 bytes | NBA 2K3 (the five Visual Concepts discs share the container) | ripped - **971 textures from the stored members, and the packed members now read through `vc_pack`** (425 more from the 24 MB verification slice alone); `176 + w*h + 512 == record size` is the check (`plugins/vc_iff.py`) | [formats/visual-concepts-dat.md](formats/visual-concepts-dat.md) |
| A2M `.gc` resource files: 256-slot type table at +64 (the slot index is the type), named resources, GX indexed triangle strips with a 56-byte f32 vertex; `.cp` is the same file behind a chain of sized zlib blocks | Teen Titans, Monster House, Ed Edd n Eddy, The Ant Bully, Happy Feet (5 discs, all previously producing nothing) | ripped **with textures** - Teen Titans 49 meshes / 5,461 triangles / 185 textures from one file of 1,368; Ed Edd n Eddy 46 / 13,422 / 485; Happy Feet 30 / 57,483 / 232.  Textures are a 256-byte header plus a GX mip chain (`256 + chain == resource size` is the check) and meshes bind to them by handle (`plugins/a2m_gc.py`) | [formats/a2m-gc.md](formats/a2m-gc.md) |
| Yuke's `.tex` texture directories: `u32 count`, then 32-byte entries of `name[16]`, `"tpl"`, **size then offset**; members are ordinary Nintendo TPLs | WWE Day of Reckoning 1 and 2, WrestleMania XIX and X8 (4 discs, 8,652 `.tex`, 381 MB, reporting 14 / 76 / 9 / 623 textures) | container ships (`plugins/yukes_tex.py`) - 120 sampled files gave **1,599 textures**, every member a TPL and every one decoding | [formats/yukes-tex.md](formats/yukes-tex.md) |
| Yuke's `YOBJ` meshes (`.ymg`): records found by a `0x0a000000` marker rather than a stride, **every offset pointing eight bytes before its data**, `f32` positions and normals with `normals - positions == count * 12` | WrestleMania X8, XIX, WWE Day of Reckoning 1 and 2 | ripped on X8 - **8,104 meshes, 47,090 triangles from 10 of its 732 files**, unsigned normal agreement 0.983 (`plugins/yukes_yobj.py`); **XIX reads too** (a group table of 8-byte entries, strips of 10-byte corners with vertex colours and uvs / 32768 - `0_2.ymg`: 6,545 triangles at 0.989).  **Day of Reckoning reads since 2026-09-03**: version 4 behind the 24-byte `DUMY` stamp - s16 positions / 64, normals stored (nz, nx, ny) / 4096, 6-byte strip corners `u16 index, s16 u, s16 v` / 1024, one group a material, TEV stages naming the `.tex` pack's TPLs (`000_0.ymg`: a wrestler, 3,542 triangles, 18 textures, 0.908); XIX's version 3 has the same material and name tables, so its groups now split by material and bind the pack's TPLs too | [formats/yukes-yobj.md](formats/yukes-yobj.md) |
| Edge of Reality `index.ind` + `.arc`: sibling directory, alternating category names and **sorted-hash** tables (`u32 count`, `count` sorted hashes, then `count` offset/size pairs), archive chosen by the category name truncated to eight characters and confirmed by tiling | The Sims, Shark Tale, Over the Hedge (3 discs reporting 2 textures each) | container ships (`plugins/edge_arc.py`) - **1,913 members**; 11 of 16 category/archive pairs tile to the byte.  Member formats still open | [formats/edge-of-reality-arc.md](formats/edge-of-reality-arc.md) |
| THQ `g4rc` textures: a `.rad` is a pack inside a pack whose `.rcb` leaves are plain zlib; the inflated object packs **`width-1` in bits 0-7 and `height-1` in bits 10-17** of the word at +16, checked against the CMPR mip chain | Avatar: The Last Airbender (9 archives, 699 MB that read as nothing) | ripped - **486 textures from the three smallest `.pak` alone**, 18 of 24 objects in `boot.rad` (`plugins/thq_g4rc.py`) | [formats/thq-g4rc.md](formats/thq-g4rc.md) |
| `FSYS` archives: three-pointer table, per-entry detail records, **unpacked size at +8 and stored size at +20**, members named | Pokemon Colosseum (1,852 archives, 350 MB), Pokemon XD (2,540, 1,032 MB) - both reporting nothing | container **and codec** ship (`plugins/fsys.py`) - the `LZSS` payload is Okumura's scheme (4 KB ring, LSB-first flags, set bit = literal, length `+3`); **800 members decode to their declared size with 0 failures**, and 1,936 come out of the 16 largest archives | [formats/fsys.md](formats/fsys.md) |
| Pokemon `FSYS` images: `u16` width, `u16` height, **`u8`** bits per pixel at +4 (32 = RGBA8, 16 = RGB5A3), 128-byte header, then GX pixels | Pokemon Colosseum | ripped - **1,332 textures** from its 14 largest archives, median 4.5x smoother than shuffled (`plugins/fsys_tex.py`).  XD wraps its members differently and is still open | [formats/fsys.md](formats/fsys.md) |

Status per disc: [GAME_STATUS.md](GAME_STATUS.md) (regenerated by each dump pass).
